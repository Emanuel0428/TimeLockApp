<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/status_bar</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/navigation_bar</item>
        <!-- Background color -->
        <item name="android:windowBackground">@color/background_primary</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/status_bar</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/navigation_bar</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Status bar color for splash -->
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/status_bar</item>
        <!-- Navigation bar color for splash -->
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/navigation_bar</item>
    </style>
</resources>